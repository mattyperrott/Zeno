apiVersion: v2
name: zeno-memory          
version: 0.1.0  

dependencies:
  # Bitnami PostgreSQL (vendored)
  - name: postgresql
    version: 15.2.2
    repository: "file://charts"

  # ChromaDB 0.6.1 (vendored)
  - name: chromadb   
    version: 0.6.1
    repository: "file://charts"